# dumbbell

周期的に幅が変化する2次元チャネル内で、電気双極子の性質を持つ棒状粒子(ダンベル型粒子)が行うブラウン運動を数値シミュレーションするプログラムです。一定の外力と一定の電場のもとでの平均初通過時間・非線形移動度・有効拡散係数を、GPU(CUDA)を用いた大規模アンサンブル計算で求めます。

物理モデルの詳細は [docs/model.md](docs/model.md) を、計算機環境は [docs/architecture.md](docs/architecture.md) を参照してください。

## 必要環境

- Rust (2024 edition)
- CUDA Toolkit 12.x (`nvcc` が必要) と NVIDIA GPU
- GPUがない環境では CPU 版としてビルド可能(後述)

用意されている[Dev Container](.devcontainer/devcontainer.json)を使うと、上記の環境がすぐに整います。

## 使い方

### 1. 設定ファイルを書く

シミュレーションの定数はすべて TOML ファイルで指定します。リポジトリ直下の [config.toml](config.toml) が設定例です。

```toml
delta_t = 4e-7          # 時間刻み幅 Δt
time = 100.0            # 総シミュレーション時間 T
k = 1.5e6               # 壁のばね定数 K
ensemble_size = 30000   # 1ケースあたりのアンサンブル平均のサンプル数
output_dir = "example/" # 結果をまとめて出力するフォルダ

# 以下の4つはリストで指定し、その全組み合わせ(直積)が実行される
f   = [10.0, 20.0]   # 一定外力(x方向)
c_1 = ["1/2", 2.0]   # 電場パラメータ C1 = βEp/l (分数表記も可)
c_2 = ["-1/3", -3.0] # 電場パラメータ C2 = ΔαE/p (分数表記も可)
l   = [0.04, 0.08]   # 棒の長さ

[gpu]                  # 省略可
# ids = [3, 1, 2, 0]   # 使用するGPU(省略時は全GPU)
# tasks_per_gpu = 4    # GPUあたりの同時実行ケース数(省略時は4)
```

### 2. シミュレーションを実行する

```sh
cargo run --release                          # ./config.toml を使って全ケースを実行
cargo run --release -- --config sweep.toml   # 設定ファイルを指定して実行
```

ケースは全GPUのワーカーに自動的に振り分けられ、終わったものから順に結果が書き出されます。

### 3. 結果を見る

`output_dir` に次の構造で出力されます。

```
example/
├── config.toml               # 使用した設定ファイルのコピー(再現用)
└── summary.dat               # 全ケースの結果(1ケース1行、完了順に追記)
                              #   列: f c_1 c_2 l mean_disp mu d_eff mfpt
```

各統計量の意味:

| 列 | 意味 |
| --- | --- |
| `mean_disp` | 平均変位 ⟨Δx⟩ |
| `mu` | 非線形移動度 μ = ⟨v⟩ / f |
| `d_eff` | 有効拡散係数 D_eff = (⟨Δx²⟩ − ⟨Δx⟩²) / 2T |
| `mfpt` | 平均初通過時間(周期1を進む平均時間)= 1 / \|⟨v⟩\| |

各ケースの乱数シードはパラメータの組から決定論的に導出されるため、同じ設定からは常に同じ結果が得られます(GPUでの総和の加算順序に由来する最下位桁程度の揺らぎを除く)。

### 4. アニメーションを見る

```sh
cargo run --release -- animate
```

1粒子の運動をリアルタイムに描画します。初期パラメータには設定ファイルの各リストの先頭値が使われます。

- **seed / f / C1 / C2 はGUIで変更可能**: f, C1, C2 は実行中の粒子に即座に反映され、seed は Reset ボタンを押したときに反映されます
- 初期状態(灰色の棒)と現在の状態(赤い棒)が同時に表示されます

## CPU版としてのビルド

GPUがないマシンでは、同じ物理モデルのCPU実装(rayon並列)で動かせます。小規模な動作確認・検証用です。

```sh
cargo run --release --no-default-features
```

## プロジェクト構成

```
src/
├── main.rs         # CLI(run / animate サブコマンド)
├── config.rs       # TOML設定の読み込みとパラメータ組み合わせへの展開
├── simulation.rs   # 物理モデルのCPU実装(アニメーションとCPU版で使用)
├── simulation.cu   # 物理モデルのGPU実装(CUDAカーネル)
├── runner.rs       # 全ケースの実行と結果の書き出し(GPU/CPUバックエンド)
└── renderer.rs     # アニメーション表示(egui/eframe)
docs/
├── model.md        # 物理モデルの導出と定式化
└── architecture.md # 実行マシンのハードウェア構成
```

## 性能メモ(NVIDIA A100での実測)

- 1カーネル(1ケース、3万粒子)ではA100を使い切れないため、**GPUあたり4ケースを別ストリームで同時実行**して飽和させています(`tasks_per_gpu` の既定値): GPUを4つ使う場合、速度低下せずに16ケースを同時に実行できます
- 飽和スループットはA100 1枚あたり約 1.3×10¹⁰ [steps/s]
  - 時間の目安:
    ```
    完了: f-10_c1-1_c2--0.333333_l-0.04 (946.8秒)
    完了: f-10_c1-2_c2--0.333333_l-0.08 (950.0秒)
    完了: f-10_c1-2_c2--0.333333_l-0.04 (953.5秒)
    完了: f-10_c1-1_c2--0.333333_l-0.08 (961.8秒)
    完了: f-20_c1-1_c2--0.333333_l-0.04 (1572.0秒)
    完了: f-20_c1-2_c2--0.333333_l-0.08 (1574.9秒)
    完了: f-20_c1-2_c2--0.333333_l-0.04 (1583.5秒)
    完了: f-20_c1-1_c2--0.333333_l-0.08 (1595.9秒)
    完了: f-10_c1-2_c2--3_l-0.08 (2028.6秒)
    完了: f-10_c1-1_c2--3_l-0.04 (2036.7秒)
    完了: f-10_c1-2_c2--3_l-0.04 (2051.1秒)
    完了: f-10_c1-1_c2--3_l-0.08 (2067.6秒)
    完了: f-20_c1-1_c2--3_l-0.04 (2533.0秒)
    完了: f-20_c1-2_c2--3_l-0.08 (2532.7秒)
    完了: f-20_c1-2_c2--3_l-0.04 (2547.5秒)
    完了: f-20_c1-1_c2--3_l-0.08 (2571.8秒)
    16/16 (100%) 全16ケース完了 (2573.5秒) → data/  
    ```
